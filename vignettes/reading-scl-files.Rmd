---
title: "Reading Scala .scl Files"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Reading Scala .scl Files}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(setharophone)
```

```{r}
parse_scale <- function(v) {
  fractional::fractional(vapply(
    c(" 1/1", v),
    FUN = function(x) eval(parse(text = x)),
    FUN.VALUE = 1
  ))
}
lcmd <- function(v) {
  numbers::mLCM(fractional::denominators(v))
}

carlos_harm <-
  readLines("d:/znmeb/Downloads/Scala scales/scl/carlos_harm.scl") |>
  grep(pattern = "/", value = TRUE) |>
  parse_scale()
(carlos_harm)
(lcmd(carlos_harm) * carlos_harm)

carlos_super <-
  readLines("d:/znmeb/Downloads/Scala scales/scl/carlos_super.scl") |>
  grep(pattern = "/", value = TRUE) |>
  parse_scale()
(carlos_super)
(lcmd(carlos_super) * carlos_super)

```

